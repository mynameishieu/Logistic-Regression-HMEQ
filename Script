import os
import subprocess

def create_github_folder(repo_owner, repo_name, folder_name, github_token):
    """
    Creates a new folder (directory) in a GitHub repository.

    Args:
        repo_owner (str): The owner of the GitHub repository.
        repo_name (str): The name of the GitHub repository.
        folder_name (str): The name of the folder to create.
        github_token (str): Your GitHub personal access token with 'repo' scope.

    Returns:
        bool: True if the folder was created successfully, False otherwise.
    """
    try:
        # Construct the URL for creating a directory via the GitHub API.
        api_url = f"https://api.github.com/repos/{repo_owner}/{repo_name}/contents/{folder_name}"

        # Construct the JSON payload for the API request.  We will commit an empty .gitkeep file to create the directory.
        data = {
            "message": f"Create folder: {folder_name}",
            "content": "", #empty file content
            "committer": {
                "name": "GitHub Folder Creator",
                "email": "foldercreator@example.com"
            }
        }
